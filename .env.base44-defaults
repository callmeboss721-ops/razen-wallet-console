# Development placeholders — overridden by /run/base44/app.env (platform secrets).
# The app boots on PGLite (embedded Postgres) when DATABASE_URL is unset,
# and runs TMN in sim mode when no TMN_* credentials are provided.
RAZEN_PUBLIC_URL=https://razen-wallet-console.vercel.app
TMN_MODE=sim
